"use client";

import { useMemo, useState } from "react";
import { Bell, BookOpen, CheckCircle2, CircleDollarSign, ClipboardCheck, GraduationCap, LayoutDashboard, Menu, MessageSquare, Plus, Receipt, Search, ShieldCheck, Users, Wallet, X } from "lucide-react";
import { demoAnnouncements, demoAttendanceTrend, demoFeeTrend, demoPayments, demoSchool, demoStats, demoStudents, roleLabels, type DemoRole, type DemoStudent } from "@/lib/demo/data";

type View = "overview" | "students" | "attendance" | "grades" | "finance" | "messages";
const nav: Record<DemoRole, { id: View; label: string; icon: any }[]> = {
  admin: [{ id: "overview", label: "Dashboard", icon: LayoutDashboard }, { id: "students", label: "Students", icon: Users }, { id: "attendance", label: "Attendance", icon: ClipboardCheck }, { id: "grades", label: "Academics", icon: GraduationCap }, { id: "finance", label: "Finance", icon: CircleDollarSign }, { id: "messages", label: "Announcements", icon: MessageSquare }],
  teacher: [{ id: "overview", label: "My Dashboard", icon: LayoutDashboard }, { id: "students", label: "My Students", icon: Users }, { id: "attendance", label: "Attendance", icon: ClipboardCheck }, { id: "grades", label: "Grades", icon: GraduationCap }, { id: "messages", label: "Announcements", icon: MessageSquare }],
  bursar: [{ id: "overview", label: "Finance Dashboard", icon: LayoutDashboard }, { id: "students", label: "Fee Accounts", icon: Users }, { id: "finance", label: "Payments", icon: Wallet }, { id: "messages", label: "Announcements", icon: MessageSquare }],
  parent: [{ id: "overview", label: "Parent Dashboard", icon: LayoutDashboard }, { id: "students", label: "My Child", icon: Users }, { id: "attendance", label: "Attendance", icon: ClipboardCheck }, { id: "grades", label: "Results", icon: GraduationCap }, { id: "finance", label: "Fees", icon: Wallet }, { id: "messages", label: "School Updates", icon: Bell }],
};
function money(n:number){return `GHS ${n.toLocaleString("en-GH",{minimumFractionDigits:2})}`}
function Pill({status}:{status:DemoStudent["status"]}){const c=status==="Present"?"bg-emerald-50 text-emerald-700":status==="Late"?"bg-amber-50 text-amber-700":"bg-rose-50 text-rose-700";return <span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${c}`}>{status}</span>}
function Stat({label,value,hint,icon:Icon}:{label:string;value:string;hint:string;icon:any}){return <div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex justify-between gap-4"><div><p className="text-sm font-medium text-slate-500">{label}</p><p className="mt-2 text-2xl font-bold text-slate-900">{value}</p><p className="mt-1 text-xs text-slate-500">{hint}</p></div><div className="rounded-xl bg-slate-100 p-2.5 text-slate-700"><Icon className="h-5 w-5"/></div></div></div>}
function Bars({values}:{values:number[]}){const max=Math.max(...values);return <div className="flex h-32 items-end gap-1.5">{values.map((v,i)=><div key={i} className="flex-1 rounded-t-md bg-slate-800/80" style={{height:`${Math.max(12,(v/max)*100)}%`}} title={`${v}%`}/>)}</div>}
function Overview({role,students,setView}:{role:DemoRole;students:DemoStudent[];setView:(v:View)=>void}){const count=role==="parent"?1:role==="teacher"?students.length:demoStats.students;const att=role==="parent"?students[0]?.attendance??96:role==="teacher"?93:demoStats.attendance;return <div className="space-y-6"><section className="rounded-2xl bg-slate-950 p-6 text-white sm:p-8"><p className="text-sm text-slate-300">{demoSchool.term}</p><h2 className="mt-2 text-2xl font-bold sm:text-3xl">{role==="parent"?"Parent Portal":role==="teacher"?"Class Teacher Workspace":role==="bursar"?"Finance & Bursary":"School Administration"}</h2><p className="mt-2 max-w-2xl text-sm text-slate-300">A realistic preview of EduCore using deterministic local sample data. No Supabase connection, no external notifications and no real student records.</p></section><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat label="Students" value={count.toLocaleString()} hint={role==="parent"?"1 child linked":"Active enrolments"} icon={Users}/><Stat label="Attendance" value={`${att}%`} hint="Current period" icon={ClipboardCheck}/><Stat label="Fees Collected" value={money(demoStats.feesCollected)} hint="Second Term" icon={CircleDollarSign}/><Stat label="Outstanding" value={money(demoStats.feesOutstanding)} hint="Sample accounts" icon={Wallet}/></div><div className="grid gap-6 xl:grid-cols-[1.4fr_1fr]"><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex justify-between"><div><h3 className="font-bold">Attendance trend</h3><p className="text-sm text-slate-500">Last 14 school days</p></div><button onClick={()=>setView("attendance")} className="text-sm font-semibold">Open</button></div><div className="mt-7"><Bars values={demoAttendanceTrend}/></div></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-bold">Fee collections</h3><p className="text-sm text-slate-500">Illustrative weekly totals</p><div className="mt-5 space-y-3">{demoFeeTrend.map((v,i)=><div key={i}><div className="mb-1 flex justify-between text-xs text-slate-500"><span>Week {i+1}</span><span>{money(v*1000)}</span></div><div className="h-2 rounded-full bg-slate-100"><div className="h-2 rounded-full bg-slate-800" style={{width:`${v/112*100}%`}}/></div></div>)}</div></div></div><div className="grid gap-6 xl:grid-cols-2"><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex justify-between"><h3 className="font-bold">Recent activity</h3><button onClick={()=>setView("finance")} className="text-sm font-semibold">View all</button></div>{demoPayments.map(p=><div key={p.id} className="flex justify-between gap-3 border-b border-slate-100 py-3 text-sm"><div><p className="font-semibold">{p.student}</p><p className="text-xs text-slate-500">{p.method} · {p.date}</p></div><span className="font-bold text-emerald-700">+{money(p.amount)}</span></div>)}</div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-bold">School updates</h3>{demoAnnouncements.map(a=><div key={a.title} className="mt-3 rounded-xl bg-slate-50 p-4"><p className="text-sm font-semibold">{a.title}</p><p className="mt-1 text-xs text-slate-500">{a.audience} · {a.date}</p></div>)}</div></div></div>}
function Students({role,students}:{role:DemoRole;students:DemoStudent[]}){const[q,setQ]=useState("");const list=useMemo(()=>students.filter(s=>`${s.name} ${s.className}`.toLowerCase().includes(q.toLowerCase())).slice(0,role==="parent"?1:10),[students,q,role]);return <div className="space-y-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><h2 className="text-xl font-bold">{role==="parent"?"My child":"Students"}</h2><p className="text-sm text-slate-500">Search sample records.</p></div><div className="relative w-full sm:w-80"><Search className="absolute left-3 top-2.5 h-4 w-4 text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} className="w-full rounded-xl border border-slate-200 py-2 pl-9 pr-3 text-sm" placeholder="Search name or class"/></div></div><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Student</th><th className="px-5 py-3">Class</th><th className="px-5 py-3">Attendance</th><th className="px-5 py-3">Average</th><th className="px-5 py-3">Balance</th><th className="px-5 py-3">Today</th></tr></thead><tbody className="divide-y divide-slate-100">{list.map(s=><tr key={s.id}><td className="px-5 py-4 font-semibold">{s.name}<div className="text-xs font-normal text-slate-400">{s.id}</div></td><td className="px-5 py-4">{s.className}</td><td className="px-5 py-4">{s.attendance}%</td><td className="px-5 py-4 font-bold">{s.average}%</td><td className="px-5 py-4">{money(s.balance)}</td><td className="px-5 py-4"><Pill status={s.status}/></td></tr>)}</tbody></table></div></div></div>}
function Attendance({role,students,onToggle}:{role:DemoRole;students:DemoStudent[];onToggle:(id:string)=>void}){const list=students.slice(0,role==="parent"?1:8);return <div className="space-y-5"><div><h2 className="text-xl font-bold">Attendance</h2><p className="text-sm text-slate-500">8 September 2026 · local demo interaction.</p></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm space-y-2">{list.map(s=><button key={s.id} disabled={role==="parent"} onClick={()=>onToggle(s.id)} className="flex w-full items-center justify-between rounded-xl border border-slate-100 p-4 text-left hover:bg-slate-50 disabled:cursor-default"><div><p className="font-semibold">{s.name}</p><p className="text-xs text-slate-500">{s.className} · {s.attendance}%</p></div><Pill status={s.status}/></button>)}</div></div>}
function Grades({role,students}:{role:DemoRole;students:DemoStudent[]}){const list=students.slice(0,role==="parent"?1:8);return <div className="space-y-5"><div><h2 className="text-xl font-bold">Academic results</h2><p className="text-sm text-slate-500">Sample published term results.</p></div><div className="grid gap-4 sm:grid-cols-3"><Stat label="Class average" value={`${Math.round(list.reduce((a,s)=>a+s.average,0)/list.length)}%`} hint="Selected students" icon={GraduationCap}/><Stat label="Highest" value={`${Math.max(...list.map(s=>s.average))}%`} hint="Current term" icon={BookOpen}/><Stat label="Published" value="100%" hint="Demo records" icon={CheckCircle2}/></div><div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="overflow-x-auto"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Student</th><th className="px-5 py-3">English</th><th className="px-5 py-3">Mathematics</th><th className="px-5 py-3">Science</th><th className="px-5 py-3">Overall</th><th className="px-5 py-3">Grade</th></tr></thead><tbody className="divide-y divide-slate-100">{list.map(s=><tr key={s.id}><td className="px-5 py-4 font-semibold">{s.name}</td><td className="px-5 py-4">{Math.max(55,s.average-4)}%</td><td className="px-5 py-4">{Math.min(99,s.average+3)}%</td><td className="px-5 py-4">{Math.max(50,s.average-1)}%</td><td className="px-5 py-4 font-bold">{s.average}%</td><td className="px-5 py-4">{s.average>=80?"A":s.average>=70?"B":s.average>=60?"C":"D"}</td></tr>)}</tbody></table></div></div></div>}
function Finance({role,students,onPay}:{role:DemoRole;students:DemoStudent[];onPay:(id:string)=>void}){const[selected,setSelected]=useState(students.find(s=>s.balance>0)?.id??students[0].id);return <div className="space-y-5"><div className="flex flex-col gap-3 sm:flex-row sm:items-end sm:justify-between"><div><h2 className="text-xl font-bold">{role==="parent"?"School fees":"Finance & payments"}</h2><p className="text-sm text-slate-500">Payments are simulated locally and never reach a provider.</p></div>{role!=="parent"&&<button onClick={()=>onPay(selected)} className="inline-flex items-center gap-2 rounded-xl bg-slate-900 px-4 py-2.5 text-sm font-semibold text-white"><Plus className="h-4 w-4"/>Record demo payment</button>}</div><div className="grid gap-4 sm:grid-cols-3"><Stat label="Collected" value={money(demoStats.feesCollected)} hint="Second Term" icon={CircleDollarSign}/><Stat label="Outstanding" value={money(demoStats.feesOutstanding)} hint="Sample accounts" icon={Wallet}/><Stat label="Receipts" value="1,248" hint="Generated in demo" icon={Receipt}/></div><div className="grid gap-6 xl:grid-cols-[1fr_1.3fr]"><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-bold">Fee accounts</h3>{students.slice(0,8).map(s=><button key={s.id} onClick={()=>setSelected(s.id)} className={`mt-2 w-full rounded-xl border p-3 text-left ${selected===s.id?"border-slate-400 bg-slate-50":"border-slate-100"}`}><div className="flex justify-between gap-3"><span className="font-semibold">{s.name}</span><span className="font-bold">{money(s.balance)}</span></div><span className="text-xs text-slate-500">{s.className}</span></button>)}</div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-bold">Recent receipts</h3>{demoPayments.map(p=><div key={p.id} className="flex justify-between gap-3 border-b border-slate-100 py-3 text-sm"><div><p className="font-semibold">{p.receipt} · {p.student}</p><p className="text-xs text-slate-500">{p.method} · {p.date}</p></div><span className="font-bold text-emerald-700">{money(p.amount)}</span></div>)}</div></div></div>}
function Messages(){return <div className="space-y-5"><div><h2 className="text-xl font-bold">Announcements</h2><p className="text-sm text-slate-500">Sample school communication.</p></div>{demoAnnouncements.map(a=><article key={a.title} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex justify-between gap-4"><div><h3 className="font-bold">{a.title}</h3><p className="mt-2 text-sm text-slate-500">Audience: {a.audience}</p></div><span className="text-xs text-slate-400">{a.date}</span></div></article>)}<div className="rounded-2xl border border-dashed border-slate-300 bg-slate-50 p-6 text-center"><Bell className="mx-auto h-6 w-6 text-slate-500"/><p className="mt-2 font-semibold">Notifications stay local in demo mode</p><p className="mt-1 text-sm text-slate-500">No SMS, WhatsApp or email provider is contacted.</p></div></div>}
export default function DemoApp(){const[role,setRole]=useState<DemoRole>("admin");const[view,setView]=useState<View>("overview");const[open,setOpen]=useState(false);const[toast,setToast]=useState("");const[students,setStudents]=useState(demoStudents);const show=(m:string)=>{setToast(m);setTimeout(()=>setToast(""),2200)};const toggle=(id:string)=>{setStudents(s=>s.map(x=>x.id===id?{...x,status:x.status==="Present"?"Absent":"Present"}:x));show("Attendance updated in demo mode")};const pay=(id:string)=>{setStudents(s=>s.map(x=>x.id===id?{...x,balance:Math.max(0,x.balance-500)}:x));show("Demo payment recorded · balance updated")};const content=view==="overview"?<Overview role={role} students={students} setView={setView}/>:view==="students"?<Students role={role} students={students}/>:view==="attendance"?<Attendance role={role} students={students} onToggle={toggle}/>:view==="grades"?<Grades role={role} students={students}/>:view==="finance"?<Finance role={role} students={students} onPay={pay}/>:<Messages/>;return <div className="min-h-screen bg-slate-50"><div className="flex min-h-screen"><aside className={`fixed inset-y-0 left-0 z-40 w-72 transform border-r border-slate-200 bg-white transition-transform lg:static lg:translate-x-0 ${open?"translate-x-0":"-translate-x-full"}`}><div className="flex h-full flex-col"><div className="flex items-center justify-between border-b border-slate-100 p-5"><div className="flex items-center gap-3"><div className="rounded-xl bg-slate-900 p-2 text-white"><GraduationCap className="h-5 w-5"/></div><div><div className="font-bold">EduCore</div><div className="text-xs text-slate-400">Interactive demo</div></div></div><button onClick={()=>setOpen(false)} className="lg:hidden"><X/></button></div><div className="border-b border-slate-100 p-4"><p className="px-2 text-[11px] font-bold uppercase tracking-wider text-slate-400">Experience a role</p><div className="mt-2 grid grid-cols-2 gap-2">{(Object.keys(roleLabels) as DemoRole[]).map(r=><button key={r} onClick={()=>{setRole(r);setView("overview");setOpen(false)}} className={`rounded-xl px-3 py-2 text-xs font-semibold ${role===r?"bg-slate-900 text-white":"bg-slate-100 text-slate-600"}`}>{roleLabels[r]}</button>)}</div></div><nav className="flex-1 space-y-1 p-4">{nav[role].map(({id,label,icon:Icon})=><button key={id} onClick={()=>{setView(id);setOpen(false)}} className={`flex w-full items-center gap-3 rounded-xl px-3 py-3 text-sm font-medium ${view===id?"bg-slate-900 text-white":"text-slate-600 hover:bg-slate-100"}`}><Icon className="h-4 w-4"/>{label}</button>)}</nav><div className="border-t border-slate-100 p-4"><button onClick={()=>{setStudents(demoStudents);setRole("admin");setView("overview");show("Demo reset")}} className="w-full rounded-xl border border-slate-200 px-3 py-2.5 text-sm font-semibold text-slate-600">Reset demo data</button></div></div></aside>{open&&<button aria-label="Close demo menu" className="fixed inset-0 z-30 bg-slate-950/30 lg:hidden" onClick={()=>setOpen(false)}/>}<main className="min-w-0 flex-1"><header className="sticky top-0 z-20 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="flex items-center justify-between gap-4 px-4 py-3 sm:px-6 lg:px-8"><div className="flex min-w-0 items-center gap-3"><button onClick={()=>setOpen(true)} className="rounded-xl border border-slate-200 p-2 lg:hidden"><Menu className="h-5 w-5"/></button><div><p className="font-bold">{demoSchool.name}</p><p className="text-xs text-slate-500">{demoSchool.location} · {demoSchool.motto}</p></div></div><div className="flex items-center gap-2"><span className="hidden rounded-xl bg-slate-100 px-3 py-2 text-xs font-semibold text-slate-600 sm:block">{roleLabels[role]} view</span><ShieldCheck className="h-5 w-5 text-slate-500"/></div></div></header><div className="mx-auto max-w-7xl p-4 sm:p-6 lg:p-8"><div className="mb-5 flex flex-col gap-2 rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-900 sm:flex-row sm:justify-between"><span><strong>Public demo mode.</strong> Sample data only; this session is fully local.</span><span className="font-semibold">No real student records</span></div>{content}</div></main></div>{toast&&<div className="fixed bottom-5 left-1/2 z-50 -translate-x-1/2 rounded-xl bg-slate-950 px-4 py-3 text-sm font-semibold text-white shadow-xl">{toast}</div>}</div>}
